import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  headerContainer: {
    flexDirection: 'row',
  },
  text: {
    flex: 1,
    alignItems: 'center',
  },
});

export default style;
