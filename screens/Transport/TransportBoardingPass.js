import React from 'react';
import {SafeAreaView, StatusBar} from 'react-native';

const TransportBoadingPass = () => {
  return (
    <SafeAreaView>
      <StatusBar hidden={true} />
    </SafeAreaView>
  );
};

export default TransportBoadingPass;
